# Discussion #26 — A reference to something should always be a JSON pointer and not a name (security scheme example)

- **Author:** @arno-di-loreto
- **Category:** :bulb: Ideas
- **Created:** 2022-10-05 11:47Z
- **Updated:** 2025-11-18 14:23Z
- **Labels:** Parse / Edit / Serialize
- **URL:** https://github.com/OAI/sig-moonwalk/discussions/26

---

## Body

In v3, some "reusable" elements are referenced by name instead of using a JSON pointer reference: security schemes (and their scopes), or tags for instance.

in v4, A JSON pointer reference should be the only way of referencing something:
- that makes the spec consistent (always referring to a component in the same way)
- allows defining locally some elements that weren't
- allows referencing externally some elements that weren't

Here's how it would work for security schemes (did not yet looked how it would work for scopes):

### v3

```
security:
  fooSecurity:
    # scheme usage + optional configuration
components:
  securitySchemes:
     fooSecurity:
     # scheme definition
```

### V4 proposal

```
security:
  - securityScheme:
      $ref: '#/components/securitySchemes/fooSecurity' # security scheme could be defined locally
    securityRequirements: # just using the original object name, could be something else
      # scheme usage + optional configuration
components:
  securitySchemes:
     mySecurityScheme:
     # scheme definition
```

PS: Speaking of security, we could also have reusable securityRequirements (but that's another discussion)

---

## Comments (5)

### Comment 1 — @jugaadi · 2022-10-05 16:13Z

Discriminator too?

  - **↳ Reply 1 — @kevinswiber · 2022-10-06 21:39Z**
  > We should look at deprecating `discriminator` in favor of a built-in JSON Schema approach.

  - **↳ Reply 2 — @handrews · 2022-10-06 23:29Z**
  > `propertyDependencies` (which will be in the next JSON Schema release) solves most of `discriminator`.  Some additional hinting is probably needed regarding the class name, as `discriminator` allowed that to diverge from the property value, but that's easily accomplished with annotations as part of a code gen library.

---

### Comment 2 — @handrews · 2022-10-06 23:30Z

Note that there is a large conversation about referencing happening [over at AsyncAPI](https://github.com/orgs/asyncapi/discussions/485), and while there are things involved there specific to AsyncAPI, it is very much relevant to OpenAPI (and JSON Schema) as well.


---

### Comment 3 — @handrews · 2023-01-10 19:37Z

Note that AsyncAPI is now [fixing this for security schemes](https://github.com/asyncapi/spec/pull/852).


---

### Comment 4 — @handrews · 2024-02-11 20:43Z

We're now considering going in the other direction - things should be component names and not JSON Pointers (although within JSON Schema that will only change if JSON Schema does):

* #72 
* #73


---

### Comment 5 — @handrews · 2025-10-29 22:50Z

@arno-di-loreto Referencing Security Schemes with a URI (although not the `$ref` syntax) was added in OAS v3.2, while also still allowing the component name syntax.  I believe the only thing that does not have a URI option is tag usage, where it was considered and rejected, at least for now.  I don't quite recall why or whether it might be revisited later.

  - **↳ Reply 1 — @arno-di-loreto · 2025-11-18 14:23Z**
  > @handrews Good news! For tags, I guess it's more complicated in the current state of v3 because the root-level `tags` holding tag definitions is a list. Using a `$ref` to identify a tag at the operation level would result in `#/tags/1`, which is not particularly meaningful and risks becoming invalid if the root tags are reordered. To enable using $refs for tags in V4, root tags should become an object (and we may consider moving it under `components`, but that's another concern).