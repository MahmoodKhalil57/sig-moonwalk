{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["bun"],
    "jsx": "react-jsx",
    "ignoreDeprecations": "6.0",
    "baseUrl": ".",
    "paths": { "@/*": ["frontend/*"] }
  },
  "comment": "frontend/ is a browser bundle built by `bun build` (it resolves the @/* paths); it is not part of the package's strict type-check surface — the minimal demo UI primitives are looser-typed than real shadcn/ui.",
  "include": ["src", "test"]
}
