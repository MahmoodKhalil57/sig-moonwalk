// CANDIDATE — generated by @suluk/shadcn (renderTableTsx). Not official OAS tooling.
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";

type Row = Record<string, unknown>;

export function PetTable({ rows }: { rows: Row[] }) {
  return (
    <Table>
      <TableHeader>
        <TableRow>
            <TableHead>Id</TableHead>
            <TableHead>Name</TableHead>
            <TableHead>Status</TableHead>
            <TableHead>Category Id</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {rows.map((row, i) => (
          <TableRow key={i}>
              <TableCell>{String(row["id"] ?? "")}</TableCell>
              <TableCell>{String(row["name"] ?? "")}</TableCell>
              <TableCell>{String(row["status"] ?? "")}</TableCell>
              <TableCell>{String(row["categoryId"] ?? "")}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}
