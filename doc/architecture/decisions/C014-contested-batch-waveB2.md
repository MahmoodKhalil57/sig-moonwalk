# C14. Contested-dependents batch (Wave 2B2) — parameter interdependencies, alt-schemas, auth

> **Provenance:** Candidate-fork ADR (Suluk), not a SIG decision. C010 Wave-2B2: 3 contested items that consult [C013](./C013-referencing-cluster.md), via compact per-item panels. Ledger: [`plan/facts/batch3-waveB2.bn`](../../../plan/facts/batch3-waveB2.bn). Workflow `wf_66a78f4e-eaf`.

Date: 2026-06-09

## Status

Accepted (candidate-fork), post-refutation ceilings 0.58–0.74. The #24 panel's refute stage notably used `council/CALIBRATION.md` to catch its own candidate's handrews valence-inversion (the pre-registered backtest miss) and a mis-dated/non-council citation — self-correction in action. converge clean.

## Decisions (3)

### #24 + #31 / #100 — param-interdependencies
@0.58. Parameter interdependencies (#24+#31/#100) resolve as a three-way split, ADOPTED at ceiling 0.58: simple AND complex same-location deps in pure JSON Schema 2020-12 inside the C004 per-location slots (readability is an education/idiom problem per handrews 2022-10-04/05 0024 c2, met by a tooling cookbook not a second format); cross-type VALUE-EQUALITY (path-ID==body-ID) as a SINGLE declared Moonwalk relational EXTENSION vocabulary — the exact home C013/#73 deferred here — runtime-only (C003-D1), by-stable-name (C009), grammar deferred to the fragment-grammar filing; no DSL in core (C011, NOT because handrews opposes DSLs — he is on-record receptive, so an optional translating DSL outside core is left OPEN not foreclosed). Provenance: inherited from 0020+0073+C011+0127, mizan-confirmed sole-witnessed at cap 0.62. Ceiling held to 0.58 (below the inherited frame floor) because the candidate's headline carried a fabricated 'handrews 2024-05-07' spine (the real comment is a bare title-rename), a source-contradicted handrews-anti-DSL valence inversion, and a non-council voice (karenetheridge) elevated to authority; the load-bearing query-string->instance-data-model deserialization gate is real but re-grounded on 0020 evaluative_mapping_gated_prerequisite (@0.7) + the 0127 synthesis, making query-param relational evaluation PROTOTYPE-GRADE until #127's query data-model lands. KEY DEFERRAL: the relational keyword grammar and the query-value deserialization model both leave this decision.

### #Item #122 — alternative-schemas
@0.74. C(alternative-schemas) ADOPTs the inherited out-of-scope-of-core route — alternative schema languages live in an opt-in alternative-schema EXTENSION (host-spec $vocabulary frame, no parallel OAS dialect), pure JSON Schema 2020-12 stays the sole core surface, and the draft's embedded x-oas-draft-alternativeSchema nesting is REJECTED — at ceiling 0.74; provenance is C011 s120@0.8 (mizan-confirmed SOLE-witness, declared 0.8 sits under the @>=0.85 sole-witness cap so the ceiling holds) + C013 no-parallel-dialect frame + the #122 author's own v4.0-do-it-properly disposition, with the load-bearing fix being single-source-of-truth-per-slot (0.7, council-convergent on hudlow-locality + handrews-ambiguity-is-a-bug); the candidate's elevation of Sturgeon's Option 3 to 'CANDIDATE direction' (0.62) is DEMOTED to a conditional comparative preference (0.5) because Option 3 was the author's ranked-second fallback behind the already-adopted Option-1 superset, the author's dominant signal is demand-skepticism (mechanism may be YAGNI), and Option 3's actual order-dependent 'validate-as-many-as-it-can / validator-decides-de-duping' semantics VIOLATE hudlow's decomposability prior — so only the narrow no-inline-mix-within-a-slot move survives council-clean; the key deferral is WHETHER to build the mechanism at all (recorded prior against over-investment), alongside registry contents, slot spelling, and statically-decidable conformance language for any future parallel validation.

### #69: Separation of concerns — auth-separation
@0.68. #69 Auth separation-of-concerns RESOLVED at CANDIDATE ceiling 0.68: externalize security by default (loose coupling) — operations reference security requirements by stable NAME/URI under the adopted seven-area decomposition (s141), inheriting C013 typed-component-name identity + C009 by-name-never-index + the shipped OAS-3.2 URI-reference primitive, with a C004/#20-shaped OPT-IN operation-local security pointer as the escape hatch for the rare locally-decidable-authz case. Provenance: the reference MECHANISM is solidly inherited, but the externalize-BY-DEFAULT POSTURE traces only to a ONE-VOICE #16 lean ('future motivator, not a frame-level aspect') + the handrews OP plug-in motivation + the SensibleWood OpenID-Connect exemplar, with garymazz's scope-reconciliation objection the lone unreconciled counter — hence 0.68, NOT the candidate's 0.8 (which laundered the seven-area decomposition's ceiling onto the externalization verdict and cited a non-existent Area-4 output string). KEY DEFERRALS: the scope-term/value-equality reconciliation vocabulary stays parked in the #24/#100 cluster, and the fine-grained parameter/body-dependent authz mechanism (hudlow's signature-as-authz-carrier position, which he himself flags unfinished, and which collides with his own documented static-locality blindspot) goes to Area-4 detailed design.
## Consequences

- Finishes Wave B (referencing C013 + these 3 dependents). Deferrals: exact relational keyword grammar (with #73), query/header → data-model deserialization (#127/#108), optional translating DSL (open, non-core).
- Security externalized → operations reference security by name; aligns with #141 functional-areas (C011) and the #16 externalize-security lean.
