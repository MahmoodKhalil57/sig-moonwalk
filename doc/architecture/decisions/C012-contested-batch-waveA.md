# C12. Contested batch (Wave 2A) — 8 frontier items via compact panels

> **Provenance:** Candidate-fork ADR — a decision of *this fork effort* (Suluk), not of the OpenAPI Moonwalk SIG. A C010 Wave-2A batch: 8 *contested* frontier items resolved via compact per-item panels (read-poles → resolve-with-council → adversarial-refute-and-finalize), pipelined isolated. Ledger: [`plan/facts/batch2-waveA.bn`](../../../plan/facts/batch2-waveA.bn). Workflow `wf_806e7b7b-df3`.

Date: 2026-06-09

## Status

Accepted (candidate-fork) at **post-refutation** ceilings (0.5–0.74). Each item got an adversarial refute pass that lowered ceilings where it bit; the council was consulted as a guide. **#60/61 (shorthand) did NOT finalize** (session limit) and stays OPEN. burhan-converge clean (149 claims).

## Decisions (8)

### #116 — Inheritance on paths and sublevels
@0.55. Frontier #116 (inheritance on paths and sublevels) RESOLVES as a Suluk candidate at ceiling 0.55: adopt a SINGLE optional per-level `shared` map carrying name-based map-merge inheritance down the keyed spine (path-collection -> pathItem -> operation; response-level DEFERRED), with override-vs-accumulate merge semantics declared explicitly per-property in a normative resolution algorithm rather than left implicit. Provenance is inherited from the thread's own flat-dict->wrapper reversal (arno Comment 3 under webron's Comment 2 nesting-chaos objection, both verbatim-verified) and from binding priors C003/C004/C008/C009, with three receipted deviations (single-container-not-IvanGoncharov's-dual-`default`+`shared`; wrapper-must-not-be-mandatory; resolve-before-the-C003-matcher). Adversarial refutation landed three CEILING-level bites, none fatal: the wrapper collides with C004's overturning of the unified-wrapper pole (lowered 0.6->0.52 and an explicit no-mandatory-wrapper sub-decision added so flat-key inheritance stays co-valid as the migration default); the deviation away from IvanGoncharov's `default` had its handrews-attributed leg stripped after the test_001 valence inversion (held at 0.48 on surviving support); and the resolve-before-matcher guardrail was cut 0.45->0.30 because it cannot out-rank the D1 soft guardrail it defends (mizan cap 0.139). Deferred: response-level reach (-> #17b/#108), the concrete per-property merge table, tag-inheritance, the reserved key name, and the one-surface-vs-two authoring question.

### #57 — Replace or remove discriminator
@0.6. #57 (replace/remove discriminator) resolves as a CANDIDATE HYBRID at ceiling 0.6: REMOVE the discriminator object as a load-bearing validation keyword (no normative force in Moonwalk), carry same-document one-of-N selection in standard JSON Schema 2020-12 at RUNTIME only (D1-safe, honoring the soft C004 D1 guardrail mizan caps at 0.142), adopt const-per-branch only as a dialect-agnostic DEMONSTRATION while deferring the canonical native keyword (propertyDependencies vs const vs if/then/else) to #73, drop discriminator mapping unconditionally (strongest cross-camp agreement, coheres with C009 name-based-reference), and RETAIN a stripped optional purely-advisory hint that merely names the dispatch property (the soft, contested, reversible half at 0.55). The candidate's structure was sound but three ceilings were over-stated against the verified #57 thread and were lowered: the D1-leaning frame claim (0.82->0.55, mizan over-confidence), the const-mechanism claim (0.7->0.62, handrews flagged const as schema-extension creep rather than endorsing it, jdesrosiers pointed at propertyDependencies), and the advisory-hint claim (0.6->0.55, sole uncalibrated proponent + handrews support sits on his backtest-inverted object-shape). The cross-location value-equality case (path-ID == body-ID) is DEFERRED to #73 exactly as C004 #20 deferred its cross-type value-equality deps — this is the #20 cross-cutting split applied to discriminator.

### #45 — Support newer HTTP versions (2/3)
@0.74. #45 (newer HTTP versions 2/3) resolves to the simplicity pole, HARDENED beyond the candidate: OpenAPI v4.0 describes HTTP at the version-agnostic semantic layer (RFC 9110), so wire-protocol version is never a normative core element and never a C003 signature/identity aspect — GET /foo over h1/h2/h3 is one operation, negotiated by ALPN not the document. The candidate's optional informational version-hint is WITHDRAWN: rafalkrupinski's recorded red-line ('not a version property absent a client-visible mechanism — use servers/separate documents') plus the pessimist's false-signal/drift flag plus handrews' don't-grow-the-control-surface objection are three independent council instruments against a decorative, non-actionable field, so the no-core-surface determination is the stronger-prior move and his library-selection use-case is redirected to the servers/capability model (#55), not a version field. The load-bearing functional-capability half (streaming, multiplexing, bidirectional flow, server-push) is admitted as real but DEFERRED to contested #108/#163; the perf/resilience slice follows resolved #102 to overlays. CANDIDATE ceiling 0.74 (no SIG ratification); the withdrawn-hint sub-decision is the most overturnable at 0.62 and revisitable once #108/#163/#55 land.

### #43 — Rate limits
@0.74. Frontier #43 (rate limits) resolves OUT-OF-SCOPE for core at a candidate ceiling of 0.74 (lowered from the proposed 0.82+ after adversarial refutation): the Candidate adds no core rate-limit keyword because rate-limit DEFINITION is a per-tier deployment/entitlement ("social contract") attribute that varies while the API shape is identical, so it is declined-and-routed — definition to the Overlay Spec + SLA4OAI ratesObject (MikeRalphson) and runtime conveyance to the IETF draft-ietf-httpapi-ratelimit-headers draft (ralfhandl) — exactly mirroring the C011 resolution of the sibling #102 (retry/timeout) in the same "Deployment configuration" cluster, and following the C008 route-don't-merge pattern. The refutation bit the ceilings, not the direction: the proposed candidate misframed the proposer jugaadi as conceding rate-limits-to-overlays when he conceded only the per-tier dimension and RETAINED a live per-operation-keyword ask (so the frame is directionally-convergent, not settled), and it hardened the #102 precedent to 0.83 above that ADR's own 0.75 base; both errors are corrected here by capping the resolution at 0.74 and recording the proposer's pole as routed-not-denied (re-openable only with a new deviation receipt showing the Overlay+headers route insufficient, per the darrelmiller/C008 escape clause). DEFERRED: modeling the wire-level RateLimit/Retry-After RESPONSE HEADERS a server returns is a header-documentation concern that hinges on the still-contested header-model items (#108) and is left open. No full adversarial panel was spawned (atom-0011): the corrected frame stays below the contested-panel threshold, and a disposition-level contrarian pass — which is what caught the misframe — was the right-sized move.

### #82: — Versioning not always at API granularity
@0.74. #82 resolves PERMIT-DON'T-MANDATE at a CANDIDATE ceiling 0.74: the spec MUST NOT bake API-granularity versioning into the model (handrews' negative/permissive thesis, Big Tent L16, SensibleWood + karelhusa corroborate, zero opposed), info.version stays the document-description version per its existing OAS definition (not an enforced contract version, not a routing/matching input), and NO new version keyword is added in this resolution. The original candidate's verdict survives but its deferral RATIONALE is corrected: the claim that rafalkrupinski's redundancy objection is "unrefuted" is falsified by SensibleWood L181-185, which separates the runtime-ADDRESSING axis (where redundancy bites) from the in-document DESCRIPTION axis (the thread's actual subject, which redundancy does not touch) — so the declarative-machinery question is DECLINED-FOR-NOW on conservative inherit-the-absence + handrews' soft-NO + minimalist keyword-discipline (an ASYMMETRIC lean, door explicitly open), not deferred as neutral equipoise; the future Step must perform the description-vs-addressing axis-split before adding any keyword. Two priors are tightened (only LITERAL version path-prefixes are C005-parseable; templated version segments inherit C005's open injectivity question) and one provenance overreach is downgraded (only "Big Tent" is repo-witnessed; a numbered "Principle 3" is not). Deferred: declarative version machinery, version-addressing transport binding, version-as-signature-aspect, inter-version operation relations, multi-document strategy, and the contract/document/deployment-version documentation-guidance gap.

### #76 — Organize spec into tiers
@0.6. #76 (organize spec into tiers) resolves to DECLINE normative conformance tiers (rafalkrupinski's Strict/Acceptable/Open) as a core 4.0 mechanism and ADOPT the flat-spec-with-optional-constructs pole, at a candidate ceiling of 0.6 — the strongest form of tiers is foreclosed because the fundamentals a "Strict" tier would gate on (static determinism, RFC6570 injectivity, map collections) are already settled UNIFORMLY across any tier by C003/D3, C005, C009 and may not be relaxed, leaving only feature-breadth/validation-depth, which C004/C008/C011 already route to opt-in/overlay/extension; the genuine needs are acknowledged and routed (rafalkrupinski's tooling-focus → a non-normative profile/label layer; earth2marsh's #18 authoring-boilerplate completeness, shown by the SIG record to be a distinct hand-author axis only dedup-conflated into #76, deferred as its own frame), with provenance held LOW (#76 single-voice, zero comments, zero ratification). Refutation corrected two candidate mis-reads — #18 is authoring-ergonomics not enterprise governance, and #172 routes the rule-suppression need OUT rather than supplying it — trimming sub-decision ceilings (s1 0.8→0.78, s3 0.6→0.5) without flipping the verdict; the deferrals (label vocabulary gated on #122/#upgrade, #18's own axis, per-tier backcompat gated on the upgrade mechanism, #82/#141 partitioning interaction) carry the unresolved parts.

### #17b — Response-level precedence (apiResponses / pathResponses / request-level responses) + parameterSchema allOf composition across pathItem+request
@0.62. Frontier #17b (split out of #17 by C008) resolves PARTIALLY: the parameterSchema-allOf half is UPHELD as the candidate framed it — pathItem-level allOf'd with request-level is a decidable, additive, runtime/D1-safe composition grounded directly in the initial-proposal recipe (README L170) and C004, with the value-equality/cross-type slice deferred to #73; but the response-scope half is CORRECTED away from the candidate's flat 'additive-union, reject most-specific-wins' verdict. Response scopes do compose additively by inheritance for DISTINCT responses (de-dup hoisting, README L66 + the canonical 201/404/5XX example), and a NEW explicit precedence field or manifest layers-slot is rejected on C008/C009 convergence; scope-DEPTH precedence rank is rejected on hudlow's decomposability objection. The candidate's over-reach was bundling the runtime STATUS/content-type specificity tiebreak (darrelmiller's live 'most precise MATCH wins' and the literal #44 forcing scenario) into the same rejection via a C009 name-identity argument that does not reach a runtime pattern-overlap — that tiebreak is instead DEFERRED to the C003 correlation/collision policy, to be decided jointly with the request-signature collision policy. A council misread was repaired: the candidate cited rafalkrupinski as corroborating additive union, but the actual #44 record (he is the question author) shows him proposing the OPPOSITE — a most-specific-SCOPE fallback we deviate from. Candidate-fork frame ceiling 0.62 (no SIG ratification; response half is now mostly a structured deferral resting on C003's still-open correlation step).

### #58 — Improvements for links
@0.5. #58 (Improvements for links) resolves to a SCOPED POLE-A BASELINE at a low CANDIDATE ceiling of 0.5: links stay operation-orchestration hints that reference a target operation by stable operationId/operation-name (inheriting C009's by-name rule), the removal pole (ahl's purpose-challenge + the Postman study's near-ZERO adoption — note the candidate's "0.5%" was fabricated; the source says no contracts used links) is explicitly weighed and rejected on a thin machine-readability warrant (graphql-mesh + schemathesis only), kscheirer's payload-opacity complaint (#90 c19) is acknowledged as a real but UNRESOLVED defect with its mechanism deferred wholesale (the candidate's synthetic "declare-response-by-reference" keyword is withdrawn as unwitnessed and as the inverse of what kscheirer actually proposed), and the full hypermedia-native redesign is deferred to #30 (verifiably OPEN/Wave-C). Provenance is mixed inherited/deviation over a 3-comment #58 thread plus #90 c19/c20, with mikekistler — the #58 reform-PROPOSER whose Pole-A line at #90 c20 is interrogative, not a verdict — anchoring direction at @0.45 only; mizan confirms zero pre-registered witnesses, so no cross-witness inflation is available and every ceiling sits 0.4-0.8. The refutation lowered ceilings, added the removal pole, and withdrew one synthetic mechanism without inverting the verdict's direction.
## Consequences

- 8 contested items resolved in one pipelined wave at honest, refutation-hardened ceilings; rich sub-decisions are in the workflow output, distilled here.
- Deferrals recorded: #57→#73 (replacement vocabulary), #58→#30 (resource-coupling), #116 response-level inheritance, #82 declarative version-machinery (door open).
- #116's optional-`shared` wrapper explicitly honors C004's no-mandatory-wrapper holding (the adjacent #20 panel had overturned a *mandatory* unified wrapper).
- **#60/61 shorthand** remains OPEN (session-limit casualty) — re-run or resolve next.
