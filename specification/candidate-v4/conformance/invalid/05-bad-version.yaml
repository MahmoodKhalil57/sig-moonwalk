openapi: 3.1.0                                                              # not a 4.0.x version
info: { title: x, version: "1" }
paths: { p: { requests: { r: { method: get, responses: { ok: { status: 200 } } } } } }
